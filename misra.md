# راهنمای جامع قوانین MISRA C:2012 به همراه مثال

MISRA C (Motor Industry Software Reliability Association C)

 مجموعه‌ای از قوانین و دستورالعمل‌های کدنویسی برای زبان برنامه‌نویسی C است که در ابتدا برای صنعت خودرو توسعه یافت و اکنون در صنایع هوافضا، مخابرات، دفاع، تجهیزات پزشکی و بسیاری دیگر استفاده می‌شود. هدف اصلی MISRA C افزایش ایمنی، قابلیت اطمینان و قابلیت حمل کد در سیستم‌های نهفته (Embedded Systems) است.

نسخه MISRA C:2012 در سال 2012 منتشر شد و آخرین نسخه معتبر پیش از MISRA C:2023 است. MISRA C:2012 شامل **159 قانون** (Rule) و **16 دستورالعمل** (Directive) است. پس از اصلاحیه‌ها (AMD1 و AMD2)، تعداد کل قوانین به **175** رسیده است.

---

## 📋 فهرست محتوا

1. [ساختار و دسته‌بندی قوانین](#ساختار-و-دسته‌بندی-قوانین)
2. [دستورالعمل‌ها (Directives)](#دستورالعمل‌ها-directives)
3. [قوانین (Rules) بر اساس دسته‌بندی موضوعی](#قوانین-rules-بر-اساس-دسته‌بندی-موضوعی)
4. [مثال‌های عملی از قوانین مهم](#مثال‌های-عملی-از-قوانین-مهم)
5. [نتیجه‌گیری و منابع مفید](#نتیجه‌گیری-و-منابع-مفید)

---

## ساختار و دسته‌بندی قوانین

### سطوح قوانین (Severity Levels)

قوانین MISRA C:2012 به سه سطح تقسیم می‌شوند:

| سطح | توضیح | تعداد |
|------|-------|-------|
| **Mandatory** (اجباری) | هرگز نباید نقض شود - نقض = نقص جدی | 10 |
| **Required** (ضروری) | باید رعایت شود - انحراف با مستندسازی مجاز است | ~110 |
| **Advisory** (توصیه‌ای) | برای بهبود کیفیت کد - قابل انعطاف | ~39 |

### دسته‌بندی نوع قوانین

- **Directive** (دستورالعمل): قوانین سطح بالا که به فرآیند توسعه و طراحی مرتبط هستند
- **Rule** (قانون): قوانین دقیق و قابل بررسی توسط ابزارهای تحلیل ایستا (Static Analysis)

### شماره‌گذاری قوانین

قوانین با فرمت `Rule X.Y` شماره‌گذاری می‌شوند که `X` نشان‌دهنده دسته موضوعی (موضوع اصلی) و `Y` شماره ترتیبی قانون در آن دسته است. به عنوان مثال:
- `Rule 1.1` → دسته 1 ، قانون شماره 1
- `Rule 8.5` → دسته 8 ، قانون شماره 5

---

## دستورالعمل‌ها (Directives)

دستورالعمل‌های MISRA C:2012 به مسائل سطح طراحی و فرآیند توسعه می‌پردازند. در ادامه مهم‌ترین آن‌ها را مرور می‌کنیم:

| شناسه | عنوان | سطح |
|-------|-------|------|
| **Dir 1.1** |  که خروجی برنامه به آن وابسته است باید مستند و فهمیده شود(implementation-defined)  هر رفتار وابسته به پیاده‌سازی  | Required |
| **Dir 2.1** | تمام فایل‌های منبع باید بدون خطای کامپایل، کامپایل شوند | Required |
| **Dir 3.1** | تمام کد باید به نیازمندی‌های مستند شده قابل ردیابی باشد | Required |
| **Dir 4.1** | شکست‌های زمان اجرا باید به حداقل برسند | Required |
| **Dir 4.2** | استفاده از زبان اسمبلی باید مستند شود | Advisory |
| **Dir 4.3** | کدهای اسمبلی باید کپسوله و ایزوله شوند | Required |
| **Dir 4.4** | نباید بخش‌هایی از کد را با کامنت کردن غیرفعال کرد | Advisory |
| **Dir 4.5** | شناسه‌های هم‌نام در فضای یکسان باید از نظر تایپوگرافی بدون ابهام باشند | Advisory |
| **Dir 4.6** | های نشان‌دهنده اندازه و علامت استفاده کرد typedefبه جای نوع‌های پایه عددی باید از  | Advisory |
| **Dir 4.7** | اگر تابعی اطلاعات خطا را بازمی‌گرداند، آن اطلاعات باید بررسی شوند | Required |
| **Dir 4.8** | نشود، پیاده‌سازی آن باید پنهان شود union  اگر اشاره‌گر به ساختار یا  | Advisory |
| **Dir 4.9** | در صورت امکان از تابع به جای ماکرو شبه‌تابع استفاده شود | Advisory |
| **Dir 4.10** | شدن چندباره محتویات فایل هدر انجام شود include باید اقداماتی برای جلوگیری از  | Required |
| **Dir 4.11** | اعتبار مقادیر ارسال شده به توابع کتابخانه باید بررسی شود | Required |
| **Dir 4.12** | تخصیص حافظه پویا نباید استفاده شود | Required |

---
در ادامه برای هر یک از این دستورالعمل‌ها (Directives) یک مثال کوچک و ساده آورده شده است.

---

### Dir 1.1 - رفتارهای وابسته به پیاده‌سازی باید مستند شوند

```c
/* مستندسازی: در این کامپایلر، 'int' 16 بیتی است */
int x = 32767;
x = x + 1;   /* ممکن است سرریز شود (implementation-defined) */
```

---

### Dir 2.1 - همه فایل‌ها باید بدون خطا کامپایل شوند

```c
int sum(int a, int b) { // کد باید عاری از خطای نحوی باشد
    return a + b;   /* درست */
}
// int x =   /* خطا: ناقص */
```

---

### Dir 3.1 - کد باید به نیازمندی‌ها قابل ردگیری باشد

```c
int add(int a, int b) { /* نیازمندی: REQ-001 - محاسبه جمع دو عدد */
    return a + b;   /* قابل ردگیری به REQ-001 */
}
```

---

### Dir 4.1 - شکست‌های زمان اجرا به حداقل برسند

```c
int divide(int a, int b) {
    if (b == 0) {
        return 0;   /* جلوگیری از تقسیم بر صفر */
    }
    return a / b;
}
```

---

### Dir 4.2 - استفاده از اسمبلی مستند شود

```c
int x = 4;
/* مستند: بهینه‌سازی برای ARM Cortex-M4 */
__asm volatile("NOP");   /* عملیات انتظار */
```

---

### Dir 4.3 - کد اسمبلی کپسوله شود

```c
static void wait_cycles(int n) {
    while (n--) {
        __asm volatile("NOP");   /* فقط در این تابع */
    }
}
```

---

### Dir 4.4 - کد را با کامنت غیرفعال نکنید

```c
int x = 5;
// نادرست:
// int old_func() { return 0; }

// درست: حذف کامل کد قدیمی
int new_func(void) {
    return 42;
}
```

---

### Dir 4.5 - شناسه‌های بی‌ابهام

```c
int x = 5;
// نادرست: "l" و "1" و "I" کنار هم
// int l = 1;  // گیج‌کننده

// درست:
int length = 1;   /* واضح */
int value = 1;
```

---

### Dir 4.6 -به جای نوع پایه typedef استفاده از 

```c
int x = 5;
// نادرست:
unsigned int speed;   /* اندازه نامشخص */

// درست:
typedef uint32_t speed_t;   /* دقیقاً 32 بیت بدون علامت */
speed_t current_speed;
```

---

### Dir 4.7 - بررسی اطلاعات خطا

```c
#include <stdio.h>
FILE* fp = fopen("file.txt", "r");
if (fp == NULL) {
    /* خطا را مدیریت کن */
    return;
}
```


---

### Dir 4.9 - استفاده از تابع به جای ماکرو شبه‌تابع

```c
int x = 5;
// نادرست (ماکرو):
#define SQUARE(x) ((x)*(x)) 

// درست (تابع):
static inline int square(int x) {
    return x * x;
}
```

---

### Dir 4.10 -  چندباره include جلوگیری از 

```c
// my_header.h
#ifndef MY_HEADER_H
#define MY_HEADER_H

/* محتویات هدر */

#endif
```

---

### Dir 4.11 - بررسی اعتبار ورودی توابع کتابخانه

```c
#include <string.h>
void copy(char* dest, const char* src, size_t n) {
    if (dest != NULL && src != NULL && n > 0) {
        memcpy(dest, src, n);
    }
}
```

---

### Dir 4.12 - عدم استفاده از حافظه پویا

```c
int x = 5;
// نادرست:
// int* p = (int*)malloc(10 * sizeof(int));

// درست:
static int buffer[10];   /* حافظه استاتیک */
```

---

## قوانین (Rules) بر اساس دسته‌بندی موضوعی

## دسته 1: محیط استاندارد C (Standard C Environment)

این دسته بر رعایت استانداردهای زبان C و اجتناب از رفتارهای تعریف‌نشده تمرکز دارد.

| قانون | عنوان | سطح |
|-------|-------|------|
| **Rule 1.1** | باشد و نباید از محدودیت‌های ترجمه پیاده‌سازی فراتر رود C  و محدودیت‌های استاندارد (syntax)  برنامه نباید شامل نقض نحو | Required |
| **Rule 1.2** | توصیه نمی‌شود (language extensions)  استفاده از افزونه‌های زبانی | Advisory |
| **Rule 1.3** |رخ دهد (critical unspecified)   یا رفتار بحرانی مشخص‌نشده (undefined) نباید هیچ رفتار تعریف‌نشده  | Required |

**مثال Rule 1.1**: استفاده از ویژگی‌های C11 در کامپایلر C90 نقض این قانون است.

**مثال Rule 1.3**: استفاده از مقدار متغیر uninitialized منجر به undefined behavior می‌شود و نقض این قانون است.

---

## دسته 2: کدهای استفاده‌نشده (Unused Code)

این دسته به حذف کدهای اضافی و استفاده‌نشده می‌پردازد که باعث افزایش حجم کد و کاهش خوانایی می‌شوند.

| قانون | عنوان | سطح |
|-------|-------|------|
| **Rule 2.1** | باشد (unreachable code)  پروژه نباید حاوی کد دست‌نیافتنی  | Required |
| **Rule 2.2** | وجود داشته باشد (dead code)  نباید کد مرده  | Required |
| **Rule 2.3** |استفاده‌نشده باشد (type)  پروژه نباید حاوی اعلان نوع  | Advisory |
| **Rule 2.4** | برچسب‌های استفاده‌نشده (unused tag declarations) نباید وجود داشته باشد | Advisory |
| **Rule 2.5** | ماکروهای استفاده‌نشده نباید وجود داشته باشد | Advisory |
| **Rule 2.6** |استفاده‌نشده باشد (label)  تابع نباید حاوی برچسب  | Required |
| **Rule 2.7** | در توابع نباید پارامتر استفاده‌نشده وجود داشته باشد | Advisory |

### مثال‌های کامل برای قوانین دسته 2 (کدهای استفاده‌نشده)

در ادامه، برای هر قانون یک مثال نقض (غیرهمراه) و یک مثال درست (همراه) ارائه می‌شود.

---

#### Rule 2.1 -(Unreachable Code)  کد دست‌نیافتنی 

**مثال نقض (غیرهمراه):**
```c
int test_function(int x) {
    return x * 2;
    int y = x + 10;     /* ❌ این خط هرگز اجرا نمی‌شود */
    return y;
}
```

**مثال درست (همراه):**
```c
int test_function(int x) {
    int y = x + 10;     /* ✅ کد قابل دسترس */
    return y;
}
```

---

#### Rule 2.2 - (Dead Code) کد مرده 

**مثال نقض (غیرهمراه):**
```c
void process(int value) {
    int temp = value * 2;   /* مقداردهی شده */
    temp = value + 5;       /* ❌ مقدار temp = value*2 هرگز استفاده نشد */
    printf("%d", temp);
}
```

**مثال درست (همراه):**
```c
void process(int value) {
    int temp = value + 5;   /* ✅ مقداردهی مستقیم */
    printf("%d", temp);
}
```

---

#### Rule 2.3 - اعلان نوع استفاده‌نشده

**مثال نقض (غیرهمراه):**
```c
typedef int MyInt;      /* تعریف شد */
enum Color { RED, GREEN, BLUE };  /* تعریف شد */

void process(void) {
    int x = 10;         /* ❌هرگز استفاده نشدند  MyInt و enum */
    printf("%d", x);
}
```

**مثال درست (همراه):**
```c
typedef int MyInt;      /* تعریف شد */
enum Color { RED, GREEN, BLUE };

void process(void) {
    MyInt x = 10;               /* ✅ استفاده از MyInt */
    enum Color c = RED;         /* ✅ استفاده از enum */
    printf("%d, %d", x, c);
}
```

---

#### Rule 2.4 - برچسب‌های استفاده‌نشده

**مثال نقض (غیرهمراه):**
```c
void process(int value) {
    start:              /* ❌  استفاده نشد goto برچسب تعریف شد ولی هرگز */
    if (value > 0) {
        printf("Positive");
    }
    // goto start; وجود ندارد
}
```

**مثال درست (همراه):**
```c
void process(int value) {
    start:              /* ✅ برچسب استفاده می‌شود */
    if (value > 0) {
        printf("Positive");
        value--;
        goto start;     /* ✅ استفاده از برچسب */
    }
}
```

---

#### Rule 2.5 - ماکروهای استفاده‌نشده

**مثال نقض (غیرهمراه):**
```c
#define MAX_SIZE 100
#define BUFFER_SIZE 256    /* ❌ این ماکرو هرگز استفاده نشد */

void process(void) {
    int arr[MAX_SIZE];     /*  استفاده شد  MAX_SIZE فقط  */
    arr[0] = 10;
}
```

**مثال درست (همراه):**
```c
#define MAX_SIZE 100
#define BUFFER_SIZE 256    /* ✅ استفاده می‌شود */

void process(void) {
    int arr[MAX_SIZE];
    char buffer[BUFFER_SIZE];   /* ✅ BUFFER_SIZE  استفاده از */
    arr[0] = 10;
}
```

---

#### Rule 2.6 - برچسب استفاده‌نشده در تابع

**مثال نقض (غیرهمراه):**
```c
int calculate(int x) {
    int result = x * 2;
    
    error_handler:      /* ❌ برچسب تعریف شد ولی استفاده نشد */
    return result;
}
```

**مثال درست (همراه):**
```c
int calculate(int x) {
    int result;
    
    if (x < 0) {
        result = 0;
        goto error_handler;    /* ✅ استفاده از برچسب */
    }
    
    result = x * 2;
    return result;
    
error_handler:                  /* ✅ برچسب استفاده می‌شود */
    return -1;
}
```

---

#### Rule 2.7 - پارامتر استفاده‌نشده در تابع

**مثال نقض (غیرهمراه):**
```c
void print_message(const char* msg, int flag) {
    /* ❌ هرگز استفاده نشد  flag پارامتر*/
    printf("%s\n", msg);
}
```

**مثال درست - روش اول (حذف پارامتر):**
```c
void print_message(const char* msg) {   /* ✅ پارامتر اضافی حذف شد */
    printf("%s\n", msg);
}
```

**مثال درست - روش دوم (استفاده از (void) برای کامپایلر):**
```c
void print_message(const char* msg, int flag) {
    (void)flag;          /* ✅ نشان می‌دهد که عمداً استفاده نمی‌شود */
    printf("%s\n", msg);
}
```

**مثال درست - روش سوم (استفاده واقعی):**
```c
void print_message(const char* msg, int flag) {
    if (flag) {                          /* ✅ استفاده از پارامتر */
        printf("DEBUG: %s\n", msg);
    } else {
        printf("%s\n", msg);
    }
}
```

---

### جدول خلاصه راهکارهای رفع هر قانون

| قانون | مشکل | راهکار رایج |
|-------|------|-------------|
| **2.1** | کد بعد از return یا break | حذف کد دست‌نیافتنی |
| **2.2** | مقداردهی بدون استفاده | حذف مقداردهی اضافی |
| **2.3** | نوع تعریف شده ولی استفاده نشده | حذف نوع یا استفاده از آن |
| **2.4** | برچسب بدون goto | حذف برچسب یا اضافه کردن goto |
| **2.5** | ماکرو تعریف شده بدون استفاده | حذف ماکرو یا استفاده از آن |
| **2.6** | برچسب در تابع استفاده نشده | حذف برچسب یا استفاده از goto |
| **2.7** | پارامتر تابع استفاده نشده | حذف پارامتر یا استفاده از `param`(void) |



---

## دسته 3: کامنت‌ها (Comments)


قوانین مرتبط با نحوه صحیح نوشتن کامنت در کد.

| قانون | عنوان | سطح |
|-------|-------|------|
| **Rule 3.1** | دنباله کاراکترهای `/*` و `//` نباید درون یک کامنت استفاده شوند | Required |
| **Rule 3.2** |(line splicing) کامنت نباید حاوی خطوط تلفیقی  باشند | Required |

**مثال Rule 3.1** (نقض - کامنت تودرتو):
```c
/* این یک کامنت /* با کامنت تودرتو */ است */
```

**مثال Rule 3.2** (مجاز - کامنت تک خطی):
```c
int y = 10;
// کامنت مجاز
int x = 5; // کامنت انتهای خط مجاز
```


---

## دسته 4: مجموعه کاراکترها (Character Sets)

قوانین مربوط به استفاده صحیح از کاراکترها و رشته‌ها.

| قانون | عنوان | سطح |
|-------|-------|------|
| **Rule 4.1** | دنباله‌های فرار هشت‌دهی و هگزادسیمال باید خاتمه داده شوند | Required |
| **Rule 4.2** |  توصیه نمی‌شود (trigraphs) استفاده از سه‌حرفی‌ها  | Advisory |

**مثال Rule 4.1** (مجاز - خاتمه رشته):
```c
"\x0a"      /* خاتمه درست (خاتمه با کاراکتر غیر هگز) */
"\xa"       /* خاتمه درست */
/* Non-compliant - نقض Rule 4.1 */
char str1[] = "\x1FABC";   /* کامپایلر \x1FABC را می‌خواند (5 رقم هگز!) */
/* Non-compliant */
char str2[] = "\x1F" "ABC";   /* این درست است چون با " خاتمه داده شده */

/* اما اگر اینطور بنویسیم: */
char str3[] = "\x1FABC";       /* باز هم مشکل قبلی */

/* Non-compliant - روش‌های غلط برای خاتمه */
char str4[] = "\x1f\x20";     /* این درست است - دو تا دنباله جدا */
char str5[] = "\x1f"  " ";    /* این درست است - با قطع رشته */

/* Non-compliant - روش‌های غلط برای خاتمه */
char str4[] = "\x1f\x20";     /* این درست است - دو تا دنباله جدا */
char str5[] = "\x1f"  " ";    /* این درست است - با قطع رشته */

/* Compliant - انتهای رشته */
char ok1[] = "\x0A";
char ok2[] = "\xFF";

/* Compliant - octal escape sequence (اما محدودیت‌های خاص خودش را دارد) */
char ok4[] = "\037";   /* 037 هشت‌دهی = 0x1F هگز */

```


### جدول مقایسه درست و نادرست

| کد | وضعیت | دلیل |
|-----|--------|------|
| `"\x0A"` | ✅ درست | خاتمه با " |
| `"\x0Aab"` | ❌ نادرست | `ab` جزئی از دنباله محسوب می‌شود |
| `"\x0A" "ab"` | ✅ درست | خاتمه با " سپس شروع رشته جدید |
| `"\xG"` | ❌ نادرست | G کاراکتر هگز معتبر نیست (اما این خطای کامپایل است) |
| `"\x1F"` | ✅ درست | خاتمه صحیح |
| `"\x1FABC"` | ❌ نادرست | 5 رقم هگز – نامشخص و خطرناک |
| `"\x1f\x20"` | ✅ درست | دو دنباله مجزا |



## دسته 5: شناسه‌ها (Identifiers)

قوانین برای نامگذاری متغیرها، توابع، ماکروها و تگ‌ها برای جلوگیری از تداخل و ابهام.

| قانون | عنوان | سطح |
|-------|-------|------|
| **Rule 5.1** | شناسه‌های خارجی باید متمایز باشند | Required |
| **Rule 5.2** |باید متمایز باشند scope  شناسه‌های اعلام شده در یک  | Required |
| **Rule 5.3** | خارجی را مخفی کند scope  داخلی نباید شناسه scope  شناسه‌ای در | Required |
| **Rule 5.4** | شناسه‌های ماکرو باید متمایز باشند | Required |
| **Rule 5.5** | شناسه باید با نام ماکرو متفاوت باشد | Required |
| **Rule 5.6** | باید منحصربه‌فرد باشد   typedef  نام | Required |
| **Rule 5.7** | نام تگ (ساختار/یونیون/تعداد) باید منحصربه‌فرد باشد | Required |
| **Rule 5.8** | باید منحصربه‌فرد باشند external linkage  شناسه‌های اشیاء یا توابع با  | Required |

### مثال‌های قوانین دسته 5 (شناسه‌ها)

---

#### Rule 5.1 - شناسه‌های خارجی باید متمایز باشند

**مثال نقض (دو فایل جداگانه):**
```c
/* file1.c */
int temp_value = 100;     /* شناسه خارجی */

/* file2.c */
int temp_value = 200;     /* ❌ - خطای پیوند (linker error) */
```

**مثال درست:**
```c
/* file1.c */
int temp_value1 = 100;    /* ✅ متمایز */

/* file2.c */
int temp_value2 = 200;    /* ✅ متمایز */
```

---

#### Rule 5.2 -باید متمایز باشند scope شناسه‌ها در یک 

**مثال نقض:**
```c
void func(void) {
    int count = 5;
    float count = 3.14;    /* ❌ در یک scope */
}
```

**مثال درست:**
```c
void func(void) {
    int intCount = 5;
    float floatCount = 3.14;    /* ✅ متمایز */
}
```

---

#### Rule 5.3 -خارجی scope عدم مخفی‌سازی شناسه 
**مثال نقض:**
```c
int total = 100;           /* scope خارجی (global) */

void calculate(void) {
    int total = 0;         /* ❌ مخفی کردن total سراسری */
    total = 50;
}
```

**مثال درست:**
```c
int total = 100;           /* scope خارجی */

void calculate(void) {
    int local_total = 0;   /* ✅ نام متفاوت */
    local_total = 50;
}
```

---

#### Rule 5.4 - شناسه‌های ماکرو باید متمایز باشند

**مثال نقض:**
```c
#define LIMIT 100
#define LIMIT 200     /* ❌ ماکرو تکراری */

void func(void) {
    int x = LIMIT;
}
```

**مثال درست:**
```c
#define MAX_LIMIT 100
#define MIN_LIMIT 0      /* ✅ متمایز */

void func(void) {
    int x = MAX_LIMIT;
}
```

---

#### Rule 5.5 - شناسه باید با نام ماکرو متفاوت باشد

**مثال نقض:**
```c
#define SIZE 10

int SIZE = 5;        /* ❌ متغیر با نام ماکرو یکسان */

void func(void) {
    int x = SIZE;    /* کدام؟ ماکرو یا متغیر؟ */
}
```

**مثال درست:**
```c
#define BUFFER_SIZE 10

int buffer_len = 5;      /* ✅ نام متفاوت */

void func(void) {
    int x = BUFFER_SIZE;  /* مشخص است که ماکرو است */
}
```

---

#### Rule 5.6 - باید منحصربه‌فرد باشد typedef نام

**مثال نقض:**
```c
typedef int score;
typedef float score;     /* ❌ typedef */

void func(void) {
    score s = 10;        /* کدام نوع؟ */
}
```

**مثال درست:**
```c
typedef int int_score;
typedef float float_score;   /* ✅ متمایز */

void func(void) {
    int_score s = 10;
}
```

---

#### Rule 5.7 - نام تگ (ساختار/یونیون/تعداد) باید منحصربه‌فرد باشد

**مثال نقض:**
```c
struct point { int x; int y; };
union point { int a; int b; };   /* ❌  تکراری 'point' تگ */

void func(void) {
    struct point p1;   /* ساختار */
    union point p2;    /* اتحاد - گیج‌کننده */
}
```

**مثال درست:**
```c
struct point { int x; int y; };
union point_u { int a; int b; };   /* ✅ تگ متفاوت */

struct point p1;
union point_u p2;
```

---

#### Rule 5.8 -   باید منحصربه‌فرد باشند external linkage شناسه‌های  

**مثال نقض:**
```c
/* file1.c */
int shared_data = 10;      /* external linkage */

/* file2.c */
float shared_data = 3.14;  /* ❌ اما نوع متفاوت - خطای پیوند */
```

**مثال درست:**
```c
/* file1.c */
int shared_int = 10;       /* ✅ متمایز */

/* file2.c */
float shared_float = 3.14; /* ✅ متمایز */
```

---

## جدول خلاصه سریع

| قانون | مشکل اصلی | راهکار |
|-------|-----------|--------|
| **5.1** |  در فایل‌های مختلف | پیشوند فایل به نام اضافه کنید |
| **5.2** |  در یک محدوده | نام‌های متفاوت بگذارید |
| **5.3** |  داخلی scope در | نام محلی را متفاوت انتخاب کنید |
| **5.4** |  ماکرو | از نام‌های یکتا استفاده کنید |
| **5.5** |  ماکرو و متغیر | ماکروها را با حروف بزرگ بنویسید |
| **5.6** |  typedef | پیشوند یا پسوند به نام اضافه کنید |
| **5.7** |   (struct/union) تگ| برای union از پسوند `_u` استفاده کنید |
| **5.8** |   external linkage با| نام‌های یکتا در سطح پروژه انتخاب کنید |



---

## دسته 6: نوع‌ها (Types)


### Rule 6.1: ها فقط باید با نوع مناسب تعریف شوند bit-field

**هدف:** جلوگیری از استفاده از نوع‌های نامناسب برای bit-field ها که ممکن است رفتار تعریف‌نشده (undefined) یا وابسته به کامپایلر (implementation-defined) ایجاد کند.

**نوع‌های مجاز در نسخه‌های مختلف C:**

| نسخه استاندارد | نوع‌های مجاز |
|---------------|--------------|
| **C90** |`unsigned int` و `signed int`  فقط |
| **C99 و بالاتر** | `char`، `short`، `long` (`unsigned`/`signed`)  و همچنین `unsigned int`, `signed int`, `_Bool` |

**مثال نقض (غیرمجاز):**
```c
struct Bad {
    float f : 3;      /* ❌ غیرمجاز - float مجاز نیست */
    void *p : 2;      /* ❌ غیرمجاز - اشاره‌گر مجاز نیست */
    double d : 1;     /* ❌ غیرمجاز */
};
```

**مثال درست:**
```c
struct Good {
    unsigned int flag1 : 1;   /* ✅ مجاز */
    signed int value : 4;      /* ✅ مجاز در C90 و C99 */
    _Bool status : 1;          /* ✅ مجاز در C99 و بالاتر */
    unsigned char bits : 3;    /* ✅ مجاز در C99 و بالاتر */
};
```

---

### Rule 6.2: باشد signed تک بیتی نامدار نباید از نوع bit-field 

**هدف:** جلوگیری از ابهام در مقدار دهی یک بیت signed. یک بیت signed به صورت `;signed int x : 1` فقط می‌تواند مقادیر `0` و `-1` را نگهداری کند (زیرا در نمایش مکمل دو، تک بیت به عنوان علامت تفسیر می‌شود: `0` = صفر، `1` = منفی یک). این موضوع معمولاً برخلاف انتظار برنامه‌نویس است.

**مثال نقض:**
```c
struct Bad {
    signed int flag : 1;   /* ❌ ممنوع - یک بیت signed */
};
```

**مثال درست:**
```c
struct Good {
    unsigned int flag : 1;   /* ✅ درست - یک بیت unsigned */
    signed int value : 2;    /* ✅ مجاز (2 بیت برای مقادیر -2 تا 1) */
};
```



## دسته 7: ثابت‌ها (Constants)

| قانون | عنوان | سطح |
|-------|-------|------|
| **Rule 7.1** |  و رشته‌های فرار هشت‌دهی استفاده کرد (octal)نباید از ثابت‌های هشت‌دهی  | Required |
| **Rule 7.2** | باشند u یا U   باید دارای پسوند unsigned  تمام ثابت‌های صحیح با نوع  | Required |
| **Rule 7.3** | نباید به عنوان پسوند عددی استفاده شود l  حرف کوچک | Required |
| **Rule 7.4 (اصلاح شده)** | نباید رشته به شیء اختصاص داده شود مگر اینکه نوع شیء اشاره‌گر به ثابت کاراکتری باشد | Required |

### مثال‌های قوانین دسته 7 (ثابت‌ها)

---

#### Rule 7.1 - (Octal) عدم استفاده از ثابت‌های هشت‌دهی 

**مثال نقض:**
```c
int a = 010;        /* ❌ ثابت هشت‌دهی (10 در مبنا 8 = 8 در مبنا 10) */
int b = 077;        /* ❌ هشت‌دهی */

char str[] = "\101";  /* ❌ رشته فرار هشت‌دهی (حرف 'A') */
```

**مثال درست:**
```c
int a = 10;         /* ✅ ده‌دهی (Decimal) */
int b = 63;         /* ✅ ده‌دهی */
int c = 0x10;       /* ✅ هگزادسیمال - مجاز */

char str[] = "\x41";  /* ✅ هگزادسیمال به جای هشت‌دهی */
```

---

#### Rule 7.2 - پسوند u/U برای ثابت‌های unsigned

**مثال نقض:**
```c
unsigned int x = 100;        /* ❌ ثابت 100 signed است */
unsigned long y = 50000;     /* ❌ ثابت signed */

if (x > 10U) {               /* ✅ این OK است چون 10U مجاز است */
    /* ... */
}
```

**مثال درست:**
```c
unsigned int x = 100U;       /* ✅ پسوند U */
unsigned long y = 50000UL;   /* ✅ پسوند UL */

/* برای اطمینان از تطابق نوع */
unsigned char mask = 0xFFU;  /* ✅ پسوند U */
```

---

#### Rule 7.3 - عدم استفاده از حرف کوچک l به عنوان پسوند

**مثال نقض:**
```c
long x = 100l;        /* ❌ حرف l کوچک (با 1 اشتباه می‌شود) */
long long y = 200ll;  /* ❌ ll کوچک */

unsigned long z = 300ul;  /* ❌ ul کوچک */
```

**مثال درست:**
```c
long x = 100L;        /* ✅ L بزرگ */
long long y = 200LL;  /* ✅ LL بزرگ */

unsigned long z = 300UL;  /* ✅ UL بزرگ */
```

---

#### Rule 7.4 - const  اختصاص رشته به اشاره‌گر غیر 

**مثال نقض:**
```c
char *str = "Hello";        /* ❌ (غیر const) char*  رشته به  */
char *ptr = "World";        /* ❌ خطر تغییر تصادفی */

*str = 'h';                 /* رفتار تعریف‌نشده (undefined) */
```

**مثال درست:**
```c
const char *str = "Hello";   /* ✅ const  اشاره‌گر به  */
const char *ptr = "World";   /* ✅ ایمن */

/* اگر نیاز به تغییر دارید، از آرایه استفاده کنید: */
char arr[] = "Hello";        /* ✅ قابل تغییر */
arr[0] = 'h';                /* ✅ مجاز */
```

---

## دسته 8: اعلان‌ها و تعاریف (Declarations and Definitions)

| قانون | عنوان | سطح |
|-------|-------|------|
| **Rule 8.1** | نوع باید به صراحت اعلام شود | Required |
| **Rule 8.2** |و نام پارامترها اعلام شوند prototype  توابع باید با | Required |
| **Rule 8.3** | همه اعلان‌های اشیاء و توابع باید با نام و نوع یکسان استفاده شوند | Required |
| **Rule 8.4** | اعلان سازگار باید قابل مشاهده باشد external linkage،  هنگام تعریف شیء یا تابع با | Required |
| **Rule 8.5** | متغیر یا تابع خارجی باید دقیقاً در یک فایل اعلام شود | Required |
| **Rule 8.6** | دقیق داشته باشد  external definition  باید یکexternal linkage شناسه با  | Required |
| **Rule 8.7** | تعریف نشود external linkage  اگر تابع و شیء فقط توسط یک واحد ترجمه ارجاع می‌شوند، بهتر است  | Advisory |
| **Rule 8.8** | استفاده شود internal linkage  static storage class specifier باید در همه اعلان‌های اشیاء و توابع با  | Required |

### مثال‌های قوانین دسته 8 (اعلان‌ها و تعاریف)

---

#### Rule 8.1 - نوع باید به صراحت اعلام شود

**مثال نقض:**
```c
x = 10;           /* ❌ نوع متغیر مشخص نیست  */

func() {          /* ❌ نوع بازگشتی مشخص نیست */
    return 5;
}
```

**مثال درست:**
```c
int x = 10;           /* ✅ نوع صریح int */

int func(void) {      /* ✅ نوع بازگشتی صریح */
    return 5;
}
```

---

#### Rule 8.2 - توابع با prototype و نام پارامترها اعلام شوند

**مثال نقض:**
```c
/* ❌ prototype قدیمی (K&R) - بدون نوع پارامترها */
void func();

/* ❌ بدون نام پارامترها */
int add(int, int);
```

**مثال درست:**
```c
void func(void);                    /* ✅ prototype کامل */

int add(int first, int second);     /* ✅ با نام پارامترها */

/* تعریف تابع */
int add(int first, int second) {
    return first + second;
}
```

---

#### Rule 8.3 - اعلان‌ها باید با نام و نوع یکسان باشند

**مثال نقض:**
```c
/* file1.h */
extern int count;        /* اعلان به عنوان int */

/* file2.c */
float count = 5.5;       /* ❌ نوع متفاوت (float) */
```

**مثال درست:**
```c
/* file1.h */
extern int count;        /* اعلان */

/* file2.c */
int count = 10;          /* ✅ نوع یکسان (int) */
```

---

#### Rule 8.4 - نیازمند اعلان سازگار قابل مشاهده external linkage

**مثال نقض:**
```c
/* file1.c */
int global_value = 100;   /* تعریف بدون اعلان قبلی در هدر */

/* file2.c */
extern int global_value;  /* ❌  وجود ندارد  file1.h اعلان در فایل دیگر، اما */
```

**مثال درست:**
```c
/* globals.h */
extern int global_value;   /* اعلان در هدر */

/* file1.c */
#include "globals.h"
int global_value = 100;    /* ✅ تعریف با اعلان قابل مشاهده */

/* file2.c */
#include "globals.h"       /* ✅ اعلان یکسان قابل مشاهده */
void use(void) {
    global_value = 200;
}
```

---

#### Rule 8.5 - متغیر یا تابع خارجی دقیقاً در یک فایل اعلام شود

**مثال نقض:**
```c
/* file1.c */
int shared = 10;     /* تعریف */

/* file2.c */
int shared = 20;     /* ❌ linker تعریف مجدد - خطای */
```

**مثال درست:**
```c
/* file1.c */
int shared = 10;     /* ✅ فقط یک تعریف در کل پروژه */

/* file2.c */
extern int shared;   /* ✅ فقط اعلان در فایل‌های دیگر */
```

---

#### Rule 8.6 - داشته باشدexternal  باید یک definition external linkage 

**مثال نقض:**
```c
/* file1.c */
extern int missing;   /* ❌ اعلان، اما هیچ تعریفی در کل پروژه وجود ندارد */

/* file2.c */
extern int missing;   /* فقط اعلان */
/* وجود ندارد  missing تعریف*/
```

**مثال درست:**
```c
/* file1.c */
int exists = 42;      /* ✅ تعریف */

/* file2.c */
extern int exists;    /* اعلان */
```

---

#### Rule 8.7 -برای استفاده محلی external بر internal linkage ترجیح 

**مثال نقض (کد در یک فایل .c):**
```c
int helper(void) {    /* ❌ اما فقط در این فایل استفاده می‌شود external linkage */
    return 42;
}

void main_func(void) {
    int x = helper();
}
```

**مثال درست:**
```c
static int helper(void) {   /* ✅ static (internal linkage) */
    return 42;
}

void main_func(void) {
    int x = helper();       /* دسترسی فقط در همین فایل */
}
```

---

#### Rule 8.8 - internal linkage برای static استفاده از 

**مثال نقض:**
```c
/* file1.c */
int local_counter = 0;      /* ❌اما فقط داخل این فایل استفاده می‌شود  external linkage */

static void local_func(void);  /*  static اعلان*/

void increment(void) {
    local_counter++;
}
```

**مثال درست:**
```c
/* file1.c */
static int local_counter = 0;   /* ✅ static - internal linkage */

static void local_func(void) {  /* ✅ static */
    local_counter++;
}

void increment(void) {
    local_func();
}
```

---

### جدول جمع‌بندی سریع

| قانون | مشکل اصلی | راهکار |
|:-------|:-----------|--------:|
| **8.1** | نوع پیش‌فرض ضمنی | همیشه نوع را به صراحت بنویسید |
| **8.2** |قدیمی یا بدون نام پارامتر  prototype | کامل با اسم پارامتر استفاده کنید prototype از |
| **8.3** | نوع یا نام ناسازگار در اعلان‌ها | تمام اعلان‌ها را با هدر مشترک هماهنگ کنید |
| **8.4** | تعریف بدون اعلان قابل مشاهده |کنید include  انجام دهید و هدر مربوطه را .c  تعریف را در فایل  |
| **8.5** | تعریف در چند فایل | فقط در یک فایل .c تعریف کنید، بقیه از extern استفاده کنند |
| **8.6** | اعلان بدون تعریف | حتماً یک فایل .c شامل تعریف آن شناسه باشد |
| **8.7** | برای استفاده محلی external  | استفاده کنید static از |
| **8.8** | internal linkage  برای missing static|اضافه کنید internal را به همه اعلان‌های linkage static   |

---

## دسته 9: مقداردهی اولیه (Initialization)

| قانون | عنوان | سطح |
|-------|-------|------|
| **Rule 9.1** | مقداردهی اولیه همه متغیرها قبل از استفاده | Required |
| **Rule 9.2** | اعضای ساختار/اتحاد باید به صورت صریح مقداردهی شوند | Required |
| **Rule 9.3** | آرایه‌ها باید قبل از استفاده مقداردهی شوند | Required |
| **Rule 9.4** | عناصر شیء با static storage duration باید مقداردهی شوند | Required |

### مثال‌های قوانین دسته 9 (مقداردهی اولیه)

---

#### Rule 9.1 - مقداردهی اولیه همه متغیرها قبل از استفاده

**مثال نقض (غیرهمراه):**
```c
void func(void) {
    int x;           /* مقداردهی نشده */
    if (x > 0) {     /* ❌ استفاده از مقدار نامشخص - undefined behavior */
        // ...
    }
}
```

**مثال درست (همراه):**
```c
void func(void) {
    int x = 0;       /* ✅ مقداردهی اولیه صریح */
    if (x > 0) {
        // ...
    }
}
```

---

#### Rule 9.2 - اعضای ساختار/اتحاد باید به صورت صریح مقداردهی شوند

**مثال نقض (غیرهمراه):**
```c
struct Point {
    int x;
    int y;
};

void func(void) {
    struct Point p;     /* ❌ اعضا مقداردهی نشده‌اند */
    p.x = 5;            /* y هنوز مقدار نامشخص دارد */
}
```

**مثال درست (همراه):**
```c
struct Point {
    int x;
    int y;
};

void func(void) {
    struct Point p = {0, 0};     /* ✅ مقداردهی صریح همه اعضا */
    /* یا */
    struct Point p2 = {.x = 5, .y = 10};  /* ✅ مقداردهی نام‌دار */
}
```

---

#### Rule 9.3 - آرایه‌ها باید قبل از استفاده مقداردهی شوند

**مثال نقض (غیرهمراه):**
```c
void func(void) {
    int arr[5];          /* ❌ آرایه مقداردهی نشده */
    arr[0] = 10;         /* سایر عناصر (1 تا 4) مقدار نامشخص دارند */
}
```

**مثال درست (همراه):**
```c
void func(void) {
    int arr[5] = {0};    /* ✅ همه عناصر صفر مقداردهی می‌شوند */
    /* یا */
    int arr2[5] = {1, 2, 3, 4, 5};   /* ✅ مقداردهی کامل */
}
```

---

#### Rule 9.4 - باید مقداردهی شوند static storage duration  عناصر شیء با 

**مثال نقض (غیرهمراه):**
```c
static int counter;   /* ❌  می‌گوید صریح بنویس   MISRA  پیش‌فرض صفر می‌شود ولی(طبق استاندارد C) */
```

**مثال درست (همراه):**
```c
static int counter = 0;   /* ✅ مقداردهی صریح */
static int flag = 1;      /* ✅ مقداردهی غیرصفر نیز صریح */
```

**نکته:** در استاندارد C، متغیرهای static به صورت خودکار صفر مقداردهی می‌شوند، اما MISRA برای خوانایی و اطمینان بیشتر، مقداردهی صریح را الزامی می‌کند.

---

### جدول خلاصه

| قانون | مشکل | راهکار |
|:-------|:------|--------:|
| **9.1** | استفاده از متغیر محلی مقداردهی‌نشده | `int x = 0;` |
| **9.2** | عضو ساختار مقداردهی‌نشده | `struct s var = {0};` یا مقداردهی نام‌دار |
| **9.3** | عناصر آرایه مقداردهی‌نشده | `int arr[5] = {0};` |
| **9.4** | static بدون مقداردهی صریح | `static int x = 0;` |

---

## دسته 10: نوع اساسی (Essential Type) 

این دسته از قوانین MISRA برای جلوگیری از خطاهای ناشی از تبدیل نوع (type conversion) و رفتارهای نامشخص در عبارات طراحی شده است.

| قانون | عنوان | سطح |
|-------|-------:|------:|
| **Rule 10.1** | عملوندها در عملیات حسابی نباید نوع اساسی متفاوتی داشته باشند | Required |
| **Rule 10.2** | باشد (essentially Boolean) نباید از نوع اساسی بولی `switch`  عبارت در | Required |
| **Rule 10.3** | نباید مقدار عبارت را به شیء با نوع اساسی باریک‌تر یا از نوع متفاوت اختصاص داد | Required |
| **Rule 10.4** | هر دو عملوند در عملگرهای `&&` و `\\` باید از نوع اساسی بولی باشند | Required |
| **Rule 10.5** |باید از نوع اساسی بولی باشد  `if`  عبارت شرط | Required |
| **Rule 10.6** |  نباید از نوع اساسی بولی باشد `return` عبارت بعد از  | Required |
| **Rule 10.7** |اگر عملوندی در عملگرهای بیتی **(~, &, \ , ^, <<, >>)** انتظار نوع بیتی دارد، نمی‌تواند از نوع اساسی بولی باشد | Required |
| **Rule 10.8** |شده نباید از نوع اساسی متفاوت باشد cast  مقدار عبارت  | Required |

---

### مثال‌های کوتاه برای هر قانون

#### Rule 10.1 - عملوندها با نوع اساسی متفاوت در عملیات حسابی

**مثال نقض:**
```c
unsigned int u = 10;
signed int s = 5;
int r = u + s;   /* ❌ عملوندها: unsigned و signed - نوع اساسی متفاوت */
```

**مثال درست:**
```c
unsigned int u = 10;
unsigned int s = 5;
unsigned int r = u + s;   /* ✅ هر دو unsigned */
```

---

#### Rule 10.2 - از نوع بولی نباشد switch عبارت
**مثال نقض:**
```c
int flag = 1;
switch (flag > 0) {   /* ❌ از نوع بولی است  (flag > 0) عبارت*/
    case 0: break;
    case 1: break;
}
```

**مثال درست:**
```c
int value = 2;
switch (value) {      /* ✅ عدد صحیح غیر بولی */
    case 0: break;
    case 1: break;
    case 2: break;
}
```

---

#### Rule 10.3 - اختصاص به نوع باریک‌تر یا متفاوت

**مثال نقض:**
```c
unsigned long long big = 0xFFFFFFFFFFFFFFFFULL;
unsigned short small = big;   /* ❌ اختصاص به نوع باریک‌تر (اتلاف داده) */
```

**مثال درست:**
```c
unsigned long long big = 0xFFFFFFFFFFFFFFFFULL;
unsigned short small = (unsigned short)(big & 0xFFFF); /* ✅ cast صریح با محدودیت */
```

---

#### Rule 10.4 - عملوندهای && و || باید بولی باشند

**مثال نقض:**
```c
int x = 5, y = 10;
if (x && y) { }      /* ❌  ( مجاز است C هرچند در ) از نوع بولی نیستند x و y */
```

**مثال درست:**
```c
int x = 5, y = 10;
if ((x != 0) && (y != 0)) { }   /* ✅ عبارات بولی */
```

---

#### Rule 10.5 -  باید بولی باشد if عبارت شرط 

**مثال نقض:**
```c
int ptr = &value;
if (ptr) { }        /* ❌  عدد صحیح است، نه بولی ptr*/
```

**مثال درست:**
```c
int *ptr = &value;
if (ptr != NULL) { }   /* ✅ عبارت بولی */
```

---

#### Rule 10.6 - نباید بولی باشد return عبارت

**مثال نقض:**
```c
int check(int a, int b) {
    return (a > b);   /* ❌ int  بازگشت مقدار بولی (0 یا 1) اما نوع برگشتی  */
}
```

**مثال درست:**
```c
int check(int a, int b) {
    if (a > b) return 1;
    else return 0;    /* ✅ مقدار عددی صریح */
}
```

---

#### Rule 10.7 - عملوند عملگرهای بیتی نباید بولی باشد

**مثال نقض:**
```c
_Bool flag = 1;
int result = flag << 2;   /* ❌ روی نوع بولی  shift عملگر*/
```

**مثال درست:**
```c
unsigned int mask = 1;
int result = mask << 2;   /* ✅ نوع بیتی مناسب */
```

---

#### Rule 10.8 - شده نباید نوع اساسی متفاوت داشته باشد cast مقدار

**مثال نقض:**
```c
unsigned int u = 10;
float f = (float)u;    /* ❌(نوع اساسی متفاوت) unsigned به float  از  cast */
```

**مثال درست:**
```c
unsigned int u = 10;
unsigned long l = (unsigned long)u;   /* ✅  در خانواده عدد صحیح بدون علامت cast*/
```

---

### نکات مهم دسته 10

- **نوع اساسی (Essential Type)** به انواع ساده شده‌ای گفته می‌شود که MISRA برای تحلیل عبارات تعریف می‌کند: بولی، کاراکتری، علامت‌دار، بدون علامت، ممیز شناور و ...
- هدف اصلی **جلوگیری از تبدیل‌های ضمنی خطرناک** و **افزایش خوانایی** است.
- در صورت نیاز به تبدیل نوع، از **cast صریح** استفاده کنید و محدودیت‌ها را رعایت نمایید.

---

## دسته 11: تبدیل نوع اشاره‌گر (Pointer Type Conversion)

| قانون | عنوان | سطح |
|-------|-------|------|
| **Rule 11.1** | تبدیل بین اشاره‌گر به تابع و هر نوع غیر تابعی ممنوع است | Required |
| **Rule 11.2** | تبدیل بین اشاره‌گر به شیء و اشاره‌گر به نوع متفاوت ممنوع است | Required |
| **Rule 11.3** | تبدیل بین اشاره‌گر و نوع صحیح ممنوع است (intptr_t/uintptr_t به استثنای تبدیل به ) | Required |
| **Rule 11.4** | تبدیل بین اشاره‌گر به توابع باید فقط به اشاره‌گر به تابع انجام شود | Required |
| **Rule 11.5** |نباید به اشاره‌گر تابع تبدیل شود void  اشاره‌گر  | Required |
| **Rule 11.6** | cast نباید const یا volatile را از نوع اشاره‌شده حذف کند | Required |
| **Rule 11.7** | cast نباید const یا volatile را از نوع اشاره‌گر به تابع حذف کند | Required |
| **Rule 11.8** | cast نباید const یا volatile را از نوع اشاره‌شده توسط اشاره‌گر حذف کند | Required |
| **Rule 11.9** | استفاده شود null  ماکرو NULL باید برای مقدار اشاره‌گر  | Required |


---

### Rule 11.1 - تبدیل اشاره‌گر به تابع به غیر تابع

**مثال نقض:**
```c
void func(void);
int *p = (int*)func;   /* ❌ تبدیل تابع به اشاره‌گر شیء */
```

**مثال درست:**
```c
void func(void);
void (*fp)(void) = func;   /* ✅ همان نوع تابع */
```

---

### Rule 11.2 - تبدیل اشاره‌گر به شیء با نوع متفاوت

**مثال نقض:**
```c
int x = 10;
float *p = (float*)&x;   /*  ❌ float* به int*  */
```

**مثال درست:**
```c
int x = 10;
int *p = &x;             /* ✅ همان نوع */
```

---

### Rule 11.3 - تبدیل اشاره‌گر و نوع صحیح

**مثال نقض:**
```c
int *p = &x;
unsigned int addr = (unsigned int)p;   /* ❌ اشاره‌گر به عدد صحیح */
```

**مثال درست (با uintptr_t):**
```c
#include <stdint.h>
int *p = &x;
uintptr_t addr = (uintptr_t)p;   /* ✅ استثنا مجاز */
```

---

### Rule 11.4 - تبدیل بین اشاره‌گر به توابع با نوع تابع متفاوت

**مثال نقض:**
```c
void func1(int);
void (*fp)(void) = (void(*)(void))func1;   /* ❌ نوع تابع متفاوت */
```

**مثال درست:**
```c
void func1(int);
void (*fp)(int) = func1;   /* ✅ دقیقاً همان نوع */
```

---

### Rule 11.5 - به اشاره‌گر تابع void* تبدیل

**مثال نقض:**
```c
void *vp = NULL;
void (*fp)(void) = (void(*)(void))vp;   /* ❌ void* به تابع */
```

**مثال درست:**
```c
void (*fp)(void) = NULL;   /* ✅ مستقیم */
```

---

### Rule 11.6 - از نوع اشاره‌شده const/volatile حذف

**مثال نقض:**
```c
const int ci = 10;
int *p = (int*)&ci;   /* ❌  const حذف*/
```

**مثال درست:**
```c
const int ci = 10;
const int *p = &ci;   /* ✅  const نگهداری*/
```

---

### Rule 11.7 - از اشاره‌گر به تابع const/volatile حذف

**مثال نقض:**
```c
void func(void);
void (* const fp)(void) = func;   /* اشاره‌گر ثابت */
void (*fp2)(void) = (void(*)(void))fp;   /* ❌از اشاره‌گر  const حذف*/
```

**مثال درست:**
```c
void func(void);
void (* const fp)(void) = func;
const void (*fp2)(void) = fp;   /* ✅مجاز است  const اضافه کردن */
```

---

### Rule 11.8 - از نوع اشاره‌شده توسط اشاره‌گر const/volatile حذف

**مثال نقض:**
```c
const char *str = "Hello";
char *p = (char*)str;   /* ❌ از کاراکتر اشاره‌شده  const حذف*/
```

**مثال درست:**
```c
const char *str = "Hello";
const char *p = str;    /* ✅  const حفظ */
```

---

### Rule 11.9 - null برای اشاره‌گر   NULL  استفاده از 

**مثال نقض:**
```c
int *p = 0;        /* ❌NULL  استفاده از صفر به جای */
int *q = (void*)0; /* ❌ */
```

**مثال درست:**
```c
int *p = NULL;     /* ✅ */
int *q = (void*)NULL; /* مجاز ولی بی‌نیاز */
```



---

## دسته 12: عبارات (Expressions)

| قانون | عنوان | سطح |
|-------|-------|------|
| **Rule 12.1** | وابستگی ترتیب ارزیابی عملوندها نباید در عبارات استفاده شود | Required |
| **Rule 12.2** | صحیح استفاده شوند unsigned باید فقط با عملوندهای  shift عملگرهای  | Required |
| **Rule 12.3** | عملگر کاما (,) نباید استفاده شود | Required |
| **Rule 12.4** | عملگر & در عبارت بولی نباید استفاده شود مگر اینکه عملوند بیتی باشد | Required |

---

### Rule 12.1 - وابستگی به ترتیب ارزیابی عملوندها

**مثال نقض:**
```c
int a = 1;
int b = (a++) + a;   /* ❌ است a++ و a نتیجه وابسته به ترتیب ارزیابی */
```

**مثال درست:**
```c
int a = 1;
int temp = a;
a++;
int b = temp + a;    /* ✅ ترتیب مشخص */
```

---

### Rule 12.2 -باشند unsigned باید shift عملوندهای 

**مثال نقض:**
```c
int x = -8;
int y = x >> 2;      /* ❌(رفتار implementation-defined) signed int روی shift  */
```

**مثال درست:**
```c
unsigned int x = 8U;
unsigned int y = x >> 2;   /* ✅ unsigned روی shift */
```

---

### Rule 12.3 - عملگر کاما ممنوع

**مثال نقض:**
```c
int a = (1, 2, 3);   /* ❌ (مقدار a = 3) استفاده از عملگر کاما  */
```

**مثال درست:**
```c
int a = 3;           /* ✅ مقدار مستقیم */
/* یا */
int b;
b = 1;
b = 2;
b = 3;
```

---

### Rule 12.4 - در عبارت بولی & استفاده از  

**مثال نقض:**
```c
int flags = 0x05;
if (flags & 0x04) { }   /* ❌  بیتی در بستر بولی (مقصود && بوده؟) &*/
```

**مثال درست (اگر مقصود بیتی است، باید صریحاً مقایسه شود):**
```c
int flags = 0x05;
if ((flags & 0x04) != 0) { }   /* ✅ تبدیل به بولی با مقایسه */
```

**یا اگر مقصود منطقی بود:**
```c
int flags = 0x05;
if (flags && 0x04) { }   /* ❌این هم مشکل دارد: */
if ((flags != 0) && (0x04 != 0)) { } /* ولی بهتر است از متغیرهای بولی استفاده شود */
```

---

### نکات مهم

- **Rule 12.1** از رفتارهای تعریف‌نشده ناشی از تغییر متغیر در یک عبارت جلوگیری می‌کند.
- **Rule 12.2** به دلیل اینکه shift روی اعداد علامت‌دار ممکن است نتایج وابسته به کامپایلر بدهد.
- **Rule 12.3** عملگر کاما خوانایی را کاهش می‌دهد و اغلب اشتباه زا است.
- **Rule 12.4** اطمینان می‌دهد که در شرایط بولی از عملگرهای منطقی (`&&`, `||`) استفاده شود نه بیتی (`&`, `|`).

---

## دسته 13: عوارض جانبی (Side Effects)

| قانون | عنوان | سطح |
|-------|-------|------|
| **Rule 13.1** | عملگرهای افزایش/کاهش نباید با سایر عملگرهای دارای عوارض جانبی ترکیب شوند | Required |
| **Rule 13.2** | مقدار یک عبارت نباید وابسته به ترتیب ارزیابی عوارض جانبی باشد | Required |
| **Rule 13.3** | عملگرهای افزایش/کاهش نباید در عبارات پیچیده استفاده شوند | Required |
| **Rule 13.4** | نتیجه عملگر انتساب نباید استفاده شود | Required |
| **Rule 13.5** | نباید عملوندی با عوارض جانبی داشته باشد sizeof عملگر| Required |
| **Rule 13.6** |نباید صفر باشد  (VLA)  آرایه در زمان اجرا size| Required |



### Rule 13.1 - ترکیب افزایش/کاهش با عملگرهای دارای عوارض جانبی

**مثال نقض:**
```c
int x = 5;
int y = ++x + x--;   /* ❌ افزایش و کاهش با + ترکیب شده */
```

**مثال درست:**
```c
int x = 5;
++x;
int y = x + x;
x--;
```

---

### Rule 13.2 - وابستگی به ترتیب ارزیابی عوارض جانبی

**مثال نقض:**
```c
int x = 1;
int y = (x++) * (x);   /* ❌ وابسته است  x++ و x نتیجه به ترتیب ارزیابی */
```

**مثال درست:**
```c
int x = 1;
int temp = x;
x++;
int y = temp * x;      /* ✅ ترتیب مشخص */
```

---

### Rule 13.3 - افزایش/کاهش در عبارات پیچیده

**مثال نقض:**
```c
int arr[] = {1, 2, 3};
int i = 0;
int x = arr[++i] + arr[++i];   /* ❌  در عبارت پیچیده ++i */
```

**مثال درست:**
```c
int arr[] = {1, 2, 3};
int i = 0;
i++;
int a = arr[i];
i++;
int b = arr[i];
int x = a + b;
```

---

### Rule 13.4 - استفاده از نتیجه عملگر انتساب

**مثال نقض:**
```c
int a, b = 5;
a = (b = 10) + 2;   /* ❌ (b=10) استفاده از نتیجه انتساب */
```

**مثال درست:**
```c
int a, b;
b = 10;
a = b + 2;          /* ✅ انتساب جداگانه */
```

---

### Rule 13.5 -  با عملوند دارای عوارض جانبی sizeof

**مثال نقض:**
```c
int x = 5;
size_t s = sizeof(x++);   /* ❌ممنوع  sizeof  هرگز اجرا نمی‌شود اما عوارض جانبی در x++ */
```

**مثال درست:**
```c
int x = 5;
size_t s = sizeof(x);     /* ✅ بدون عوارض جانبی */
x++;                      /* افزایش جداگانه */
```

---

### Rule 13.6 -  صفر size با VLA

**مثال نقض:**
```c
void func(int n) {
    int arr[n];   /* ❌  صفر(رفتار تعریف‌نشده) size با VLA باشد، n == 0 اگر صفر  */
}
```

**مثال درست:**
```c
void func(int n) {
    if (n == 0) return;   /* ✅ بررسی و جلوگیری */
    int arr[n];
}
```

---

### نکات مهم دسته 13

- **هدف کلی:** جلوگیری از ابهام در ترتیب ارزیابی و رفتارهای تعریف‌نشده
- **Rule 13.4:** نتیجه `=` نباید به عنوان زیرعبارت استفاده شود
- **Rule 13.5:**  در زمان کامپایل ارزیابی می‌شود، داشتن عوارض جانبی گمراه‌کننده است `sizeof`
- **Rule 13.6:** مجاز نیست C  با اندازه صفر در استاندارد VLA


---

## دسته 14: جریان کنترل (Control Flow)

| قانون | عنوان | سطح |
|-------|-------|------|
| **Rule 14.1** | حداقل یک شرط توقف داشته باشد  should حلقه| Required |
| **Rule 14.2** | و عبارات افزایش/شرط داشته باشد counter باید یک  for حلقه | Required |
| **Rule 14.3** | ( در انتهای تابع برای پاکسازی singleton استثنا: پرش ) نباید استفاده شود goto  | Required |
| **Rule 14.4** |باید دقیقاً یک عبارت بولی داشته باشند  if  ساختارهای شرطی | Required |

---

### Rule 14.1 - حلقه باید شرط توقف داشته باشد

**مثال نقض:**
```c
while (1) {      /* ❌ شرط توقف مشخص نیست */
    // do something
}
```

**مثال درست:**
```c
int i = 0;
while (i < 10) {   /* ✅ شرط توقف مشخص */
    i++;
}
```

---

### Rule 14.2 -  و عبارات افزایش/شرط داشته باشد counter باید یک  for حلقه 

**مثال نقض:**
```c
int i = 0;
for ( ; ; ) {      /* ❌  شرط و افزایش  counter،بدون*/
    if (i++ > 10) break;
}
```

**مثال درست:**
```c
for (int i = 0; i < 10; i++) {   /* ✅ counter، شرط، افزایش */
    // ...
}
```

---

### Rule 14.3 -(با استثنا) goto عدم استفاده از 

**مثال نقض:**
```c
void func(void) {
    goto error;    /* ❌ goto برای کنترل جریان عادی */
    error:
    // ...
}
```

**مثال درست (استثنا - پاکسازی در انتها):**
```c
int func(void) {
    int *p = malloc(10);
    if (!p) goto cleanup;
    // ...
    cleanup:
    free(p);
    return 0;
}
```

---

### Rule 14.4 -  باید دقیقاً یک عبارت بولی داشته باشد if

**مثال نقض:**
```c
int x = 5;
if (x) { }         /* ❌ (مجاز است C هرچند در ) بولی نیست x */
```

**مثال درست:**
```c
int x = 5;
if (x != 0) { }    /* ✅ عبارت بولی صریح */
```


---

## دسته 15: جمله SWITCH

| قانون | عنوان | سطح |
|-------|-------:|------:|
| **Rule 15.1** |نباید استفاده شود expression switch  ماکرو در  | Required |
| **Rule 15.2** |  باید حداقل یک دسته داشته باشد switch | Required |
| **Rule 15.3** | یا دسته خالی پایان یابد break باید با  case هر| Required |
| **Rule 15.4** | استفاده شود switch  قاعده باید فقط در انتهای default| Required |
| **Rule 15.5** |داشته باشد  default باید یک  switch | Required |
| **Rule 15.6** |استفاده شود  fall-through  نباید با چینش switch| Advisory |

---

### Rule 15.1 - عدم استفاده از ماکرو در عبارت switch

**مثال نقض:**
```c
#define VALUE 2
switch (x) {
    case VALUE: break;   /* ❌ مجاز نیست expression ماکرو در */
}
```

**مثال درست:**
```c
int val = 2;
switch (x) {
    case 2: break;       /* ✅ ثابت مستقیم */
}
```

---

### Rule 15.2 -  باید حداقل یک دسته داشته باشد switch 

**مثال نقض:**
```c
switch (x) { }   /* ❌   ندارد  case یا default هیچ دسته  */
```

**مثال درست:**
```c
switch (x) {
    case 0: break;   /* ✅ حداقل یک دسته */
}
```

---

### Rule 15.3 -  یا دسته خالی پایان یابد break باید با  case هر

**مثال نقض:**
```c
switch (x) {
    case 1:
        a = 5;       /* ❌  break (fall-through) بدون*/
    case 2:
        b = 6;
        break;
}
```

**مثال درست:**
```c
switch (x) {
    case 1:
        a = 5;
        break;       /* ✅ break */
    case 2:
        break;       /* ✅ دسته خالی */
}
```

---

### Rule 15.4 -  استفاده شود switch  قاعده باید فقط در انتهای default

**مثال نقض:**
```c
switch (x) {
    default:         /* ❌در ابتدا  default */
        break;
    case 0:
        break;
}
```

**مثال درست:**
```c
switch (x) {
    case 0:
        break;
    default:         /* ✅  در انتها default */
        break;
}
```

---

### Rule 15.5 - داشته باشد  default باید یک  switch

**مثال نقض:**
```c
switch (x) {
    case 0: break;
    case 1: break;
    /* ❌  default بدون */
}
```

**مثال درست:**
```c
switch (x) {
    case 0: break;
    case 1: break;
    default: break;   /* ✅ default */
}
```

---

### Rule 15.6 - استفاده شود  fall-through  نباید با چینش switch

**مثال نقض:**
```c
switch (x) {
    case 0:
        a = 1;
        /* ❌ case 1 عمدی به fall-through */
    case 1:
        b = 2;
        break;
}
```

**مثال درست (استفاده از break):**
```c
switch (x) {
    case 0:
        a = 1;
        break;        /* ✅  fall-through بدون */
    case 1:
        b = 2;
        break;
}
```



---

## دسته 16: توابع (Functions)

| قانون | عنوان | سطح |
|-------|-------|------|
| **Rule 16.1** | توابع نباید بیش از ۲ پارامتر داشته باشند | Advisory |
| **Rule 16.2** | توابع نباید بیش از ۵ پارامتر ورودی داشته باشند | Advisory |
| **Rule 16.3** |(recursion ممنوع)  توابع نباید مستقیماً یا غیرمستقیم خود را فراخوانی کنند | Required |
| **Rule 16.4** | در پروژه باید منحصربه‌فرد باشد identifier  توابع  | Required |
| **Rule 16.5** | توابع نباید پارامترها را در صورت نیاز اصلاح کنند | Required |
| **Rule 16.6** | تعداد پارامتر در تعریف و اعلان تابع باید یکسان باشد | Required |
| **Rule 16.7** | (باعلان سه نقطه) اعلام شده، نباید فراخوانی شود ellipsis  تابعی که با استفاده از  | Required |


---

### Rule 16.1 - بیش از ۲ پارامتر (توصیه‌ای)

**مثال نقض:**
```c
void draw(int x, int y, int color) { }   /* ❌  سه پارامتر */
```

**مثال درست:**
```c
typedef struct { int x; int y; int color; } DrawParams;
void draw(DrawParams params) { }   /* ✅ یک پارامتر */
```

---

### Rule 16.2 - بیش از ۵ پارامتر (توصیه‌ای)

**مثال نقض:**
```c
void config(int a, int b, int c, int d, int e, int f) { }   /* ❌ شش پارامتر */
```

**مثال درست:**
```c
typedef struct { int a,b,c,d,e,f; } Config;
void config(Config cfg) { }   /* ✅ یک پارامتر */
```

---

### Rule 16.3 - recursion عدم استفاده از 

**مثال نقض:**
```c
int factorial(int n) {
    if (n <= 1) return 1;
    return n * factorial(n - 1);   /* ❌ فراخوانی مستقیم خود */
}
```

**مثال درست:**
```c
int factorial(int n) {
    int result = 1;
    for (int i = 2; i <= n; i++) result *= i;   /* ✅ حلقه به جای بازگشت */
    return result;
}
```

---

### Rule 16.4 - شناسه توابع منحصربه‌فرد در پروژه

**مثال نقض (دو فایل):**
```c
/* file1.c */
void process(void) { }

/* file2.c */
void process(void) { }   /* ❌ تابع در پروژه منحصر به فرد نیست*/
```

**مثال درست:**
```c
/* file1.c */
void process_input(void) { }

/* file2.c */
void process_output(void) { }   /* ✅ نام‌های متفاوت */
```

---

### Rule 16.5 - اصلاح نکردن پارامترها

**مثال نقض:**
```c
void to_upper(char *str) {
    while (*str) {
        *str = toupper(*str);   /* ❌ اصلاح محتوای اشاره‌شده پارامتر */
        str++;
    }
}
```

**مثال درست:**
```c
void to_upper(const char *src, char *dst) {
    
    *dst = toupper(*src);   /* ✅ را تغییر نمی‌دهد  src پارامتر*/ 
}
```

---

### Rule 16.6 - تطابق تعداد پارامتر در اعلان و تعریف

**مثال نقض:**
```c
/* اعلان در هدر */
void func(int a, int b);

/* تعریف در فایل c */
void func(int a) { }   /* ❌ تعداد پارامتر متفاوت */
```

**مثال درست:**
```c
/* اعلان */
void func(int a, int b);

/* تعریف */
void func(int a, int b) { }   /* ✅ تعداد یکسان */
```

---

### Rule 16.7 - (باعلان سه نقطه) اعلام شده، نباید فراخوانی شود ellipsis  تابعی که با استفاده از 

**مثال نقض:**
```c
#include <stdio.h>
printf("value = %d", x);   /* ❌ (printf)  فراخوانی تابع با ...  */
```

**مثال درست:**
```c
/* ellipsis استفاده از توابع ایمن‌تر یا عدم استفاده از  */
void print_int(int value) {
    /* پیاده‌سازی خاص */
}
print_int(x);   /* ✅ بدون ellipsis */
```

---

### نکات مهم دسته 16

- **Rule 16.1 \ 16.2** توصیه‌ای هستند اما برای خوانایی و تست‌پذیری مفیدند
- **Rule 16.3**  در سیستم‌های بحرانی به دلیل خطر سرریز پشته ممنوع است recursion
- **Rule 16.5** تاکید دارد که پارامترها را نباید به عنوان متغیرهای محلی تغییر داد
- **Rule 16.7**  به دلیل عدم امنیت نوعی ممنوع است ( printf, scanf مثل ) استفاده از توابع با تعداد آرگومان متغیر 



---

## دسته 17: اشاره‌گر و آرایه (Pointers and Arrays)

| قانون | عنوان | سطح |
|:-------|-------:|------:|
| **Rule 17.1** |باید رعایت شود (bounds)  ویژگی‌های آرایه  | Required |
| **Rule 17.2** | اشاره‌گرها نباید فراتر از انتهای آرایه اشاره کنند | Required |
| **Rule 17.3** | تفریق اشاره‌گرها مجاز نیست | Required |
| **Rule 17.4** | کند dereference را NULL اشاره‌گر نباید  | Required |
| **Rule 17.5** | اشاره‌گرها فقط در محدوده یکسان مقایسه شوند | Required |
| **Rule 17.6** | صفر مجاز نیست (size)  طول آرایه  | Required |
| **Rule 17.7** | مجاز است type-punning  آرایه به اشاره‌گر تبدیل شود فقط با قوانین  | Advisory |
| **Rule 17.8** |نباید در محاسبات اشاره‌گر استفاده شود (void*)  اشاره‌گر خالی  | Required |
| **Rule 17.9** |استفاده نشود dereferenced pointer   باید بر روی  sizeof| Required |

---


### Rule 17.1 - رعایت bounds آرایه

**مثال نقض:**
```c
int arr[5];
arr[5] = 10;   /* ❌ خارج از محدوده (اندیس 5 معادل ششمین عنصر) */
```

**مثال درست:**
```c
int arr[5];
arr[4] = 10;   /* ✅ آخرین عنصر مجاز (0 تا 4) */
```

---

### Rule 17.2 - اشاره‌گر فراتر از انتهای آرایه نرود

**مثال نقض:**
```c
int arr[5];
int *p = arr + 5;   /* ❌ اشاره به یک عنصر بعد از انتها */
*p = 1;             /* رفتار تعریف‌نشده */
```

**مثال درست:**
```c
int arr[5];
int *p = arr + 4;   /* ✅ اشاره به آخرین عنصر مجاز */
```

---

### Rule 17.3 - تفریق اشاره‌گرها ممنوع

**مثال نقض:**
```c
int arr[5];
int *p = &arr[0];
int *q = &arr[3];
int diff = q - p;   /* ❌ تفریق اشاره‌گرها مجاز نیست */
```

**مثال درست:**
```c
int arr[5];
size_t index = 3 - 0;   /* ✅ استفاده از اندیس به جای تفریق اشاره‌گر */
```

---

### Rule 17.4 -  کند dereference را NULL اشاره‌گر نباید

**مثال نقض:**
```c
int *p = NULL;
int x = *p;        /* ❌ dereference NULL */
```

**مثال درست:**
```c
int *p = NULL;
if (p != NULL) {
    int x = *p;    /* ✅ فقط پس از بررسی */
}
```

---

### Rule 17.5 - مقایسه اشاره‌گرها فقط در محدوده یکسان

**مثال نقض:**
```c
int arr1[5];
int arr2[5];
int *p = arr1 + 2;
int *q = arr2 + 2;
if (p < q) { }     /* ❌ مقایسه اشاره‌گرها از آرایه‌های متفاوت */
```

**مثال درست:**
```c
int arr[5];
int *p = arr + 2;
int *q = arr + 4;
if (p < q) { }     /* ✅ هر دو به یک آرایه اشاره دارند */
```

---

### Rule 17.6 - طول آرایه صفر مجاز نیست

**مثال نقض:**
```c
int arr[0];        /* ❌ آرایه با طول صفر در استاندارد C (غیرمجاز) */
```

**مثال درست:**
```c
int arr[1];        /* ✅ حداقل طول 1 */
```

---

### Rule 17.7 -(توصیه‌ای) type-punning تبدیل آرایه به اشاره‌گر فقط با 

**مثال نقض (type-punning ناایمن):**
```c
float farr[4];
int *ip = (int*)farr;   /* ❌  مانند بدون اطمینان از هم‌راستایی reinterpret_cast */
```

**مثال درست (با union یا memcpy):**
```c
union { float f; int i; } u;
u.f = 3.14;
int x = u.i;   /* ✅ type-punning مجاز در شرایط خاص */
```

---

### Rule 17.8 - در محاسبات اشاره‌گر ممنوع void* استفاده از 

**مثال نقض:**
```c
void *vp = some_ptr;
vp++;           /* ❌ (غیراستاندارد) void*محاسبه روی  */
```

**مثال درست:**
```c
char *cp = (char*)some_ptr;
cp++;           /* ✅  مجاز است char*محاسبه روی */
```

---

### Rule 17.9 - استفاده نشود dereferenced pointer   باید بر روی  sizeof

**مثال نقض:**
```c
int *p = NULL;
size_t s = sizeof(*p);   /* ❌نمادین ممنوع  dereference هرچند خطای زمان اجرا نمی‌دهد، اما */
```

**مثال درست:**
```c
int *p = NULL;
size_t s = sizeof(int);  /* ✅ استفاده مستقیم از نوع */
/* یا */
size_t t = sizeof(*p);   /* آن را نهی کرده  MISRA در برخی متون مجاز شمرده می‌شود اما */
```

---



## دسته 18: همپوشانی حافظه (Overlapping Storage)

| قانون | عنوان | سطح |
|:-------|-------:|------:|
| **Rule 18.1** | نباید بین اعضای مختلف نگاشت شود union| Required |
| **Rule 18.2** | مجاز نیست union  تفریق بین اشاره‌گرها اعضای  | Required |
| **Rule 18.3** | استفاده شود type-punning  نباید با union | Required |
| **Rule 18.4** |اشاره کند union  هیچ بایتی نباید به دو عضو اشاره‌گر یا آرایه عضو  | Required |
| **Rule 18.5** |نباید گسترش یابد malloc  محدوده تخصیص حافظه پویا با  | Required |
| **Rule 18.6** | تابع برای حافظه پویا نباید بیش از یک بار واگذار شود | Required |
| **Rule 18.7** | تخصیص حافظه پویا نباید استفاده شود | Required |
| **Rule 18.8** |نباید استفاده شود setjmp  تابع | Required |
| **Rule 18.9** | نباید استفاده شود longjmp تابع | Required |

---



### Rule 18.1 -union عدم نگاشت بین اعضای مختلف 

**مثال نقض:**
```c
union Data {
    int i;
    float f;
} u;
u.i = 10;
float x = u.f;   /* ❌ (تفسیر می‌شود float به عنوان  i مقدار) نگاشت بین اعضای مختلف */
```

**مثال درست:**
```c
union Data {
    int i;
    float f;
} u;
u.i = 10;
float x = (float)u.i;   /* ✅ تبدیل صریح به جای نگاشت */
```

---

### Rule 18.2 - مجاز نیست union تفریق اشاره‌گرها به اعضای 

**مثال نقض:**
```c
union U {
    int a;
    char b[10];
} u;
int *p = &u.a;
char *q = u.b;
ptrdiff_t diff = q - p;   /* ❌ union تفریق اشاره‌گر به اعضای مختلف  */
```

**مثال درست:**
```c
union U { int a; char b[10]; } u;
/* استفاده نکنید union از تفریق اشاره‌گر بین اعضای */
```

---

### Rule 18.3 -  type-punning برای union عدم استفاده از 

**مثال نقض:**
```c
union Pun {
    uint32_t u;
    float f;
} pun;
pun.u = 0x3f800000;
float f = pun.f;   /* ❌  */
```

**مثال درست:**
```c
uint32_t u = 0x3f800000;
float f;
memcpy(&f, &u, sizeof(f));   /* ✅ کپی بایت به بایت (مجاز ولی با احتیاط) */
```

---

### Rule 18.4 - union بایت مشترک بین دو عضو اشاره‌گر یا آرایه در 

**مثال نقض:**
```c
union U {
    int *p;
    int arr[2];
} u;
u.p = &x;
int y = u.arr[0];   /* ❌ p و arr بایت‌های مشترک بین  */
```

**مثال درست:**
```c
/*استفاده نشود  union طراحی مجدد به گونه‌ای که اعضا همپوشانی نداشته باشند یا از */
```

---

### Rule 18.5 - ممنوع malloc گسترش محدوده تخصیص 

**مثال نقض:**
```c
int *p = (int*)malloc(sizeof(int));
p = realloc(p, 2 * sizeof(int));   /* ❌  گسترش محدوده حافظه  */
```

**مثال درست:**
```c
/*  malloc/realloc استفاده از حافظه استاتیک یا با اندازه ثابت به جای */
static int arr[10];
```

---

### Rule 18.6 - تابع نباید بیش از یک بار مالک حافظه پویا شود

**مثال نقض:**
```c
void* get_buffer(void) {
    static void *buf = NULL;
    if (!buf) buf = malloc(100);
    return buf;      /* ❌ اگر چندبار فراخوانی شود، مالکیت تابع اول باقی می‌ماند */
}
```

**مثال درست:**
```c
void init_buffer(void) {
    static int initialized = 0;
    if (!initialized) {
        /* تخصیص یک بار */
        initialized = 1;
    }
}
```

---

### Rule 18.7 - عدم استفاده از تخصیص حافظه پویا

**مثال نقض:**
```c
int *p = (int*)malloc(sizeof(int) * 10);   /* ❌ malloc ممنوع */
free(p);
```

**مثال درست:**
```c
int arr[10];        /* ✅ حافظه استاتیک یا خودکار */
/* یا */
static int pool[10];
```

---

### Rule 18.8 - setjmp عدم استفاده از 

**مثال نقض:**
```c
#include <setjmp.h>
jmp_buf env;
if (setjmp(env) == 0) {   /* ❌ setjmp ممنوع */
    longjmp(env, 1);
}
```

**مثال درست:**
```c
/*setjmp/longjmp حلقه به جای   if، switch، استفاده از ساختارهای کنترلی معمولی مانند  */
```

---

### Rule 18.9 - longjmp عدم استفاده از 

**مثال نقض:**
```c
#include <setjmp.h>
jmp_buf env;
setjmp(env);
longjmp(env, 1);   /* ❌ longjmp ممنوع */
```

**مثال درست:**
```c
/* استفاده از بازگشت تابع و کدهای خطا */
int func(void) {
    if (error) return -1;
    return 0;
}
```

---

### نکات مهم دسته 18

- **قوانین 18.1 تا 18.4** تمام روش‌های reinterpret_cast میان اعضای union را ممنوع می‌کنند.
- **قوانین 18.5 تا 18.7** تخصیص حافظه پویا (malloc/calloc/realloc/free) را به کلی ممنوع می‌کنند (در سیستم‌های بحرانی).
- **قوانین 18.8 و 18.9** setjmp/longjmp را به دلیل ایجاد جریان کنترل غیرمحلی و خطرناک ممنوع می‌کنند.


---

## دسته 19: همپوشانی ماکرو (Preprocessing Macros)

| قانون | عنوان | سطح |
|:-------|-------:|------:|
| **Rule 19.1** | ماکرو را نباید با متغیرهای محلی اشتباه گرفت | Required |
| **Rule 19.2** |ماکرو باید دربرگیرنده پارامترها باشد    prototype | Required |
| **Rule 19.3** | ماکرو نباید بیش از یک دستور داشته باشد | Required |
| **Rule 19.4** | ماکرو باید از عملگر () در هنگام استفاده از پارامترها استفاده کند | Required |
| **Rule 19.5** |ارزیابی شود true/false نباید مقدار  if ماکرو در  | Required |
| **Rule 19.6** |جایگزین شوند، اگر ممکن باشد const و enum  ماکروها باید با  | Advisory |
| **Rule 19.7** | ماکروها نباید به صورت "#" یا "##" استفاده شوند | Required |
| **Rule 19.8** |های چندباره استفاده شود Include‌ ماکروها برای جلوگیری از  | Required |
| **Rule 19.9** |(const/enum به جای  ) ماکروها نباید برای ثابت‌های حسابی استفاده شوند  | Required |
| **Rule 19.10** | عملگر کاما در لیست آرگومان ماکرو ممنوع است | Required |
| **Rule 19.11** |در وسط پروژه استفاده کند #undef  ماکرو نباید از | Required |


---

### Rule 19.1 - عدم اشتباه ماکرو با متغیر محلی

**مثال نقض:**
```c
#define VAL 10
void func(void) {
    int VAL = 5;   /* ❌ ماکرو با نام متغیر محلی اشتباه می‌شود */
}
```

**مثال درست:**
```c
#define VALUE 10
void func(void) {
    int local_val = 5;   /* ✅ نام‌های متفاوت */
}
```

---

### Rule 19.2 -  ماکرو باید شامل پارامترها باشد prototype

**مثال نقض:**
```c
#define SQUARE() ((x)*(x))   /* ❌ prototype ناقص  */
```

**مثال درست:**
```c
#define SQUARE(x) ((x)*(x))   /* prototype صحیح */
#define TWO 2                  /* ماکرو بدون پارامتر مشکلی ندارد */
```

---

### Rule 19.3 - ماکرو نباید بیش از یک دستور داشته باشد

**مثال نقض:**
```c
#define SWAP(a,b) { int tmp = a; a = b; b = tmp; }   /* ❌ چند دستور */
```

**مثال درست:**
```c
#define SWAP(a,b) do { int tmp = a; a = b; b = tmp; } while(0)   /* ✅ یک دستور مرکب */
```

---

### Rule 19.4 - استفاده از پرانتز در اطراف پارامترها

**مثال نقض:**
```c
#define SQUARE(x) x * x   /* ❌ بدون پرانتز: SQUARE(1+2) = 1+2*1+2 = 5 */
```

**مثال درست:**
```c
#define SQUARE(x) ((x)*(x))   /* ✅ پرانتز اطراف هر پارامتر و کل عبارت */
```

---

### Rule 19.5 - عبارت ماکرو نباید بولی باشد

**مثال نقض:**
```c
#define IS_POSITIVE(x) (x > 0)
if (IS_POSITIVE(value)) { }   /* ❌ ماکرو به عنوان شرط بولی */
```

**مثال درست:**
```c
#define POSITIVE_RESULT(x) ((x > 0) ? 1 : 0)
int result = POSITIVE_RESULT(value);
if (result != 0) { }   /* ✅ عبارت واضح */
```

---

### Rule 19.6 - const/enum جایگزینی ماکرو با 

**مثال نقض:**
```c
#define MAX_SIZE 100   /* ❌ ماکرو برای ثابت */
```

**مثال درست:**
```c
const int MAX_SIZE = 100;   /* ✅ const */
enum { MAX_SIZE = 100 };
```

---

### Rule 19.7 - ## عدم استفاده از # و 

**مثال نقض:**
```c
#define STR(x) #x          /* ❌  stringify عملگر */
#define CONCAT(a,b) a##b   /* ❌  concatenation عملگر*/
```

**مثال درست:**
```c
/* استفاده از توابع یا روش‌های دیگر */
```

---

### Rule 19.8 -  با ماکرو include guard

**مثال درست:**
```c
/* file.h */
#ifndef FILE_H
#define FILE_H
/* محتویات هدر */
#endif
```

---

### Rule 19.9 - عدم استفاده از ماکرو برای ثابت‌های حسابی

**مثال نقض:**
```c
#define PI 3.14159   /* ❌ ماکرو برای ثابت حسابی */
```

**مثال درست:**
```c
const double PI = 3.14159;   /* ✅ const */
/* یا */
enum { FACTOR = 10 };        /* برای اعداد صحیح */
```

---

### Rule 19.10 - ممنوعیت عملگر کاما در آرگومان ماکرو

**مثال نقض:**
```c
#define SUM(a,b) ((a)+(b))
int x = SUM(1,2,3);   /* ❌ کاما اضافی */
```

**مثال درست:**
```c
int x = SUM(1,2);   /* ✅ تعداد آرگومان صحیح */
```

---

### Rule 19.11 - در وسط پروژه #undef عدم استفاده از 

**مثال نقض:**
```c
#define TEMP 100
/* ... */
#undef TEMP   /* ❌ #undef در میانه پروژه */
```

**مثال درست:**
```c
/* فقط در انتهای فایل یا در بخش‌های جداگانه اگر ضروری باشد */
#ifdef TEMP
#undef TEMP
#endif
/* اما بهتر است استفاده نشود */
```



---

## دسته 20: استاندارد کتابخانه (Standard Library)

این دسته از قوانین به استفاده ایمن از کتابخانه استاندارد C می‌پردازد.


| قانون | عنوان | سطح |
|:-------|-------:|------:|
| **Rule 20.1** | ممنوع است  `#define` و `#undef`در  reserved identifier یا macro name  استفاده از | Required |
| **Rule 20.2** | نباید اعلام شود reserved identifier یا macro name  | Required |
| **Rule 20.3** | کتابخانه مختص به توابع کتابخانه استاندارد باید استفاده شود | Required |
| **Rule 20.4** | توابع کتابخانه استاندارد نباید دوباره تعریف شوند | Required |
| **Rule 20.5** |دقیقاً باید کنترل شود (`printf`/`scanf`)  استفاده از قالب‌های ورودی‌خروجی  | Required |
| **Rule 20.6** |نباید استفاده شوند (`malloc`, `free`, `realloc`, `calloc`)  توابع کتابخانه استاندارد با حافظه دینامیک | Required |
| **Rule 20.7** | ماکروهای کتابخانه استاندارد نباید تغییر کنند | Required |
| **Rule 20.8** |   از کتابخانه استاندارد نباید تغییر کند (`typedef`) نوع | Required |
| **Rule 20.9** |  نگهدارنده شامل فایل هدر کتابخانه استاندارد استفاده شود `#ifndef`| Required |
| **Rule 20.10** | توابع کتابخانه استاندارد نباید در فایل هدر استفاده شوند | Required |
| **Rule 20.11** |باید با محدودیت ایمن باشند `sprintf`  رشته‌ها با  | Required |
| **Rule 20.12** | چندین عملیات بر روی رشته همزمان استفاده نشود | Required |

---

### Rule 20.1 -  ممنوع است  `#define` و `#undef`در  reserved identifier یا macro name  استفاده از 

**مثال نقض:**
```c
#define _STDIO_H   /* ❌ استفاده از شناسه رزرو شده (زیرخط + حرف بزرگ) */
```

**مثال درست:**
```c
#define MY_HEADER_H   /* ✅ شناسه مجاز (بدون زیرخط ابتدایی) */
```

---

### Rule 20.2 -  نباید اعلام شود reserved identifier یا macro name

**مثال نقض:**
```c
int _Counter;     /* ❌ شناسه با زیرخط ابتدایی رزرو شده است */
```

**مثال درست:**
```c
int g_counter;    /* ✅ شناسه مجاز */
```

---

### Rule 20.3 -  کتابخانه مختص به توابع کتابخانه استاندارد باید استفاده شود 

**مثال نقض:**
```c
/* بدون استفاده از کتابخانه memcpy پیاده‌سازی دستی */
void my_memcpy(void *d, const void *s, size_t n) { ... }   /*کتابخانه استفاده شود  memcpy بهتر است از */
```

**مثال درست:**
```c
#include <string.h>
memcpy(dest, src, n);   /* ✅ استفاده از تابع استاندارد */
```

---

### Rule 20.4 - عدم بازتعریف توابع کتابخانه

**مثال نقض:**
```c
int strlen(const char *s) {   /* ❌ بازتعریف تابع کتابخانه */
    return 0;
}
```

**مثال درست:**
```c
int my_strlen(const char *s) {   /* ✅ نام متفاوت */
    return 0;
}
```

---

### Rule 20.5 - printf/scanf کنترل دقیق 

**مثال نقض:**
```c
int x;
scanf("%d", x);      /* ❌ & جا افتاده (رفتار نامشخص) */
printf("%s", 123);   /* ❌ نوع ناهماهنگ */
```

**مثال درست:**
```c
int x;
scanf("%d", &x);     /* ✅ اشاره‌گر صحیح */
printf("%d", x);     /* ✅ تطابق نوع */
```

---

### Rule 20.6 - عدم استفاده از حافظه پویا

**مثال نقض:**
```c
int *p = (int*)malloc(10 * sizeof(int));   /* ❌ malloc ممنوع */
free(p);
```

**مثال درست:**
```c
int arr[10];   /* ✅ حافظه استاتیک یا خودکار */
```

---

### Rule 20.7 - عدم تغییر ماکروهای کتابخانه

**مثال نقض:**
```c
#undef NULL          /* ❌ تغییر ماکرو استاندارد */
#define NULL 0
```

**مثال درست:**
```c
/* روی ماکروهای کتابخانه نزنید  #undef یا #define هرگز*/
```

---

### Rule 20.8 -کتابخانه typedef عدم تغییر 

**مثال نقض:**
```c
typedef int size_t;   /* ❌ size_t بازتعریف */
```

**مثال درست:**
```c
#include <stddef.h>   /* ✅ استفاده از تعریف اصلی */
```

---

### Rule 20.9 -  نگهدارنده شامل فایل هدر کتابخانه استاندارد استفاده شود 

**مثال درست (برای هدر اختصاصی):**
```c
/* myheader.h */
#ifndef MYHEADER_H
#define MYHEADER_H
#include <stdio.h>   /* هدر استاندارد */
#endif
```

---

### Rule 20.10 - عدم استفاده از توابع کتابخانه در هدر

**مثال نقض:**
```c
/* myheader.h */
#include <string.h>
void foo(void) {
    strcpy(dest, src);   /* ❌ اجرای کد در هدر */
}
```

**مثال درست:**
```c
/* myheader.h */
void foo(void);   /* فقط اعلان */

/* myfile.c */
#include <string.h>
void foo(void) {
    strcpy(dest, src);   /* ✅ پیاده‌سازی در فایل .c */
}
```

---

### Rule 20.11 - باید با محدودیت ایمن باشند `sprintf`  رشته‌ها با 

**مثال نقض:**
```c
char buf[10];
sprintf(buf, "%s", "Very long string");   /* ❌ سرریز */
```

**مثال درست:**
```c
char buf[10];
snprintf(buf, sizeof(buf), "%s", "Very long string");   /* ✅ محدودیت */
```

---

### Rule 20.12 - عملیات همزمان روی رشته ممنوع

**مثال نقض:**
```c
char *p = strtok(str, ",");
char *q = strtok(str, ":");   /* ❌ تداخل در وضعیت strtok */
```

**مثال درست:**
```c
char *p = strtok(str, ",");
/* (نسخه قابل ورود مجدد) در سیستم‌های مجاز strtok_r استفاده از */
/* یا انجام عملیات به صورت متوالی و جداگانه */
```



## دسته 21: امنیت (Security)

این دسته در اصلاحیه AMD1 معرفی شده و به مسائل امنیتی می‌پردازد.

| قانون | عنوان | سطح |
|:-------|-------:|------:|
| **Rule 21.1** |ممنوع است reserved identifier یا macro name  بر روی #define و #undef | Required |
| **Rule 21.2** | نباید اعلام شود reserved identifier یا macro name | Required |
| **Rule 21.3** | کتابخانه مختص به توابع امنیتی باید استفاده شود | Required |
| **Rule 21.4** |نباید استفاده شوند gets, scanf, sprintf  توابع خطرناک مانند | Required |
| **Rule 21.5** |باید با محدودیت استفاده شوند (مثل strcpy)  توابع کتابخانه با خطاهای زمان اجرا  | Required |
| **Rule 21.6** |نباید استفاده شوند (setjmp/longjmp)  توابع استفاده از اشیاء با فراخوانی بازگشت | Required |
| **Rule 21.7** |نباید انجام شودsystem استفاده از تابع  | Required |
| **Rule 21.8** | توابع کتابخانه با اطلاعات اجرای محیط نباید استفاده شوند | Required |
| **Rule 21.9** | توابع کتابخانه با دسترسی به فایل نباید استفاده شوند | Required |
| **Rule 21.10** |نباید استفاده شوند locale  توابع کتابخانه با تنظیمات | Required |
| **Rule 21.11** |نباید استفاده شوند  signal  توابع کتابخانه با تنظیمات | Required |
| **Rule 21.12** |نباید انجام شود abort, exit, getenv  استفاده از  | Required |
| **Rule 21.13** | استفاده شود argv  نباید با پارامتر `&` | Required |
| **Rule 21.14** | نباید استفاده شوند (math errors) توابع کتابخانه با خطاهای حساب | Required |
| **Rule 21.15** | توابع کتابخانه با خطاهای زمان اجرای نامشخص نباید استفاده شوند | Required |
| **Rule 21.16** | حساست است، نباید استفاده شوند (overflow) توابع کتابخانه با هر نوع داده‌ای که به سرریز  | Required |
| **Rule 21.17** | توابع کتابخانه با هر نوع داده موقتی که به سرریز حساست نباشد، نباید استفاده شوند | Required |
| **Rule 21.18** | نباید استفاده شوند `atof` و `atoi` توابع کتابخانه  | Required |

---

### Rule 21.1 - ممنوع است reserved identifier یا macro name  بر روی #define و #undef 

**مثال نقض:**
```c
#define _STDIO_H   /* ❌ شناسه رزرو شده (زیرخط + حرف بزرگ) */
#undef NULL        /* ❌ ماکرو رزرو شده */
```

**مثال درست:**
```c
#define MY_HEADER_H   /* ✅ نام مجاز */
```

---

### Rule 21.2 -  نباید اعلام شود reserved identifier یا macro name 

**مثال نقض:**
```c
int _Counter;     /* ❌ زیرخط ابتدایی رزرو شده */
```

**مثال درست:**
```c
int g_counter;    /* ✅ */
```

---

### Rule 21.3 - استفاده از کتابخانه امنیتی به جای استاندارد

**مثال نقض:**
```c
strcpy(dest, src);   /* ❌ غیرامن - بدون بررسی طول */
```

**مثال درست:**
```c
strncpy(dest, src, sizeof(dest) - 1);   /* ✅ محدودیت */
dest[sizeof(dest)-1] = '\0';
```

---

### Rule 21.4 - توابع خطرناک ممنوع

**مثال نقض:**
```c
gets(buffer);        /* ❌ هرگز استفاده نشود */
scanf("%s", str);    /* ❌ بدون محدودیت */
sprintf(buf, "%s", str); /* ❌ سرریز */
```

**مثال درست:**
```c
fgets(buffer, sizeof(buffer), stdin);
/* snprintf استفاده از توابع محدود مانند */
```

---

### Rule 21.5 - باید با محدودیت استفاده شوند (مثل strcpy)  توابع کتابخانه با خطاهای زمان اجرا

**مثال نقض:**
```c
strcpy(dest, src);   /* ❌ سرریز احتمالی */
```

**مثال درست:**
```c
strncpy(dest, src, n);
dest[n-1] = '\0';
```

---

### Rule 21.6 - setjmp/longjmp ممنوع

**مثال نقض:**
```c
#include <setjmp.h>
jmp_buf env;
if (setjmp(env) == 0) { longjmp(env, 1); }
```

**مثال درست:**
```c
/* return استفاده از کدهای خطا و */
```

---

### Rule 21.7 - ممنوع system تابع

**مثال نقض:**
```c
system("ls -l");   /* ❌ آسیب‌پذیری امنیتی */
```

**مثال درست:**
```c
/* از فراخوانی سیستم عامل اجتناب کنید */
```

---

### Rule 21.8 - توابع با اطلاعات محیط اجرا ممنوع

**مثال نقض:**
```c
char *env = getenv("PATH");   /* ❌ */
```

**مثال درست:**
```c
/* مقادیر محیط را از روش‌های امن و مشخص دریافت کنید */
```

---

### Rule 21.9 - توابع دسترسی به فایل ممنوع

**مثال نقض:**
```c
FILE *fp = fopen("data.txt", "r");   /* ❌ */
fclose(fp);
```

**مثال درست:**
```c
/* استفاده از رابط‌های ایمن‌تر یا حذف دسترسی فایل */
```

---

### Rule 21.10 - ممنوع locale توابع

**مثال نقض:**
```c
setlocale(LC_ALL, "");   /* ❌ */
```

**مثال درست:**
```c
/* استفاده نکنید  locale از*/
```

---

### Rule 21.11 - ممنوع signal توابع

**مثال نقض:**
```c
signal(SIGINT, handler);   /* ❌ */
```

**مثال درست:**
```c
/* مکانیزم‌های دیگر برای مدیریت وقفه */
```

---

### Rule 21.12 - abort, exit, getenv ممنوع

**مثال نقض:**
```c
exit(1);     /* ❌ */
abort();     /* ❌ */
getenv("HOME"); /* ❌ */
```

**مثال درست:**
```c
return error_code;   /* ✅ */
```

---

### Rule 21.13 - ممنوع & با argv استفاده از  

**مثال نقض:**
```c
int main(int argc, char *argv[]) {
    char **p = &argv;   /* ❌ */
}
```

**مثال درست:**
```c
/*مستقیماً استفاده کنید، نه آدرس آن  argv از خود */
```

---

### Rule 21.14 - توابع با خطاهای حساب ممنوع

**مثال نقض:**
```c
double x = sqrt(-1.0);   /* ❌ domain خطای*/
```

**مثال درست:**
```c
if (x >= 0) {
    double y = sqrt(x);
}
```

---

### Rule 21.15 - توابع با خطای زمان اجرای نامشخص ممنوع

**مثال نقض:**
```c
int x = atoi("abc");   /* ❌ رفتار نامشخص (در برخی 0 برمی‌گرداند اما غیرقابل اعتماد) */
```

**مثال درست:**
```c
long x;
char *end;
x = strtol("abc", &end, 10);
if (end == "abc") { /* خطا */ }
```

---

### Rule 21.16 - توابع حساس به سرریز ممنوع

**مثال نقض:**
```c
int sum = a + b;   /* ❌ سرریز signed undefined behavior */
```

**مثال درست:**
```c
if (a > INT_MAX - b) { /* مدیریت خطا */ }
else { int sum = a + b; }
```

---

### Rule 21.17 - توابع با داده موقتی حساس به سرریز

(مشابه 21.16 - تکرار مفهوم با مثال دیگر)

**مثال نقض:**
```c
unsigned short us = 65535;
us = us + 1;   /* wrap-around (implementation-defined) */
```

**مثال درست:**
```c
unsigned int temp = (unsigned int)us + 1;
if (temp > USHRT_MAX) { /* مدیریت */ }
```

---

### Rule 21.18 - atof و atoi ممنوع

**مثال نقض:**
```c
int i = atoi("123");   /* ❌ عدم تشخیص خطا */
double d = atof("3.14");
```

**مثال درست:**
```c
char *end;
long i = strtol("123", &end, 10);
if (end == "123" || errno == ERANGE) { /* خطا */ }
```


## نتیجه‌گیری و منابع مفید

MISRA C:2012 با 175 قانون (شامل اصلاحیه‌ها) یکی از کامل‌ترین استانداردهای کدنویسی برای سیستم‌های بحرانی (safety-critical) است. این قوانین در پنج حوزه اصلی مزیت دارند: **افزایش قابلیت اطمینان، افزایش خوانایی، افزایش قابلیت حمل، افزایش قابلیت نگهداری و افزایش امنیت**.

### نکات کلیدی برای پیاده‌سازی:

1. **استفاده از ابزارهای تحلیل ایستا**: بسیاری از قوانین MISRA با ابزارهایی مانند Polyspace، Coverity، PVS-Studio، Helix QAC و غیره قابل بررسی هستند

2. **مدیریت استثناها (Deviations)**: برخی قوانین ممکن است در پروژه خاص قابل اجرا نباشند. انحرافات باید مستند شوند

3. **پوشش قوانین**: حدود 93٪ قوانین MISRA C:2012 توسط ابزارهای تحلیل ایستا قابل بررسی هستند (100٪ قوانین Mandatory)

4. **اولویت‌بندی**: ابتدا قوانین Mandatory و سپس Required را پیاده‌سازی کنید. قوانین Advisory بیشتر برای بهبود کیفیت و خوانایی کد هستند

